[package]
name = "icu4x_bridge"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["staticlib"]

[dependencies.icu_capi]
version = "2.0"
default-features = false
features = [
    "casemap",
    "compiled_data",
]
